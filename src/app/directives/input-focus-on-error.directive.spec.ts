import { InputFocusOnErrorDirective } from './input-focus-on-error.directive';

describe('InputFocusOnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFocusOnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
